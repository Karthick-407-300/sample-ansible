- name: Deploy Website to Nginx
  hosts: localhost
  become: yes

  roles:
    - nginx

  tasks:
    - name: Copy website files
      copy:
        src: "{{ playbook_dir }}/"
        dest: /var/www/html/
        remote_src: no
        exclude:
          - roles
          - inventory
          - playbook.yml
          - Jenkinsfile