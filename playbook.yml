- name: Deploy Website to Nginx
  hosts: localhost
  become: yes

  tasks:

    - name: Copy website files
      copy:
        src: site/
        dest: /var/www/html/
        remote_src: no

    - name: Restart nginx
      service:
        name: nginx
        state: restarted